{
  "name": "timeline",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "tsc && npm run sass-dev",
    "build": "tsc && npm run sass-build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --update --style=expanded scss:dist",
    "sass-build": "sass --style=expanded scss:dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "sass": "^1.81.0",
    "typescript": "^5.7.2"
  }
}
